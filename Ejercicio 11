/*Hacer un diagrama de ujo que calcule la suma de los n ́umeros pares
comprendidos entre el 10 y 50.*/
#include <stdio.h>
int par,i,par;
int sum;
int main()
{
  printf("\tEste un programa suma los numeros pares contenidos entre 10 y 50\n");
  for(i=0;i<21;i++)
  {
  par=10+(2*i);
  sum=sum+par;
  }
  printf("la suma de los numeros par que se encuentran contenidos del 10 al 50 es:%d\n",sum);
}
