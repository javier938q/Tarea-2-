/*Escribir un diagrama de ujo tal que dado como entrada un arreglo unidi-
mensional de enteros, determinar cu ́antos de ellos son positivos, negativos
o nulos.*/


#include <stdio.h>

int lista[5];
int p,n,neu,i;
int main()
{
  printf("\fEste es un programa que te ayuda saber cuantos numeros hay positivos, negativos y neutros segun una lista ingresada\n");
  printf("Igrese la lista deseada de componentes\n");
  for(i=0;i<5;i++)
  {
    scanf("%d",&lista[i]);
    printf("El %d valor es:%d\n",i+1,lista[i]);
  }
   for (i=0;i<100;i++)
    {
      if(lista[i]>0);
      {
        if(lista[i]>0)
          {
            p=p+1;
          }   
        else
        {
          n=n+1;
        }
      }
      if(lista[i]==0)
      {
       neu=neu+1;
      }
    }
  printf("hay %d numero positivo\n",p);
  printf("hay %d numero negativos\n",n);
  printf("hay %d numero neutros\n",neu);
}
 
