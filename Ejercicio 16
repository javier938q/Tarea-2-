/*Construir un diagrama de ujo para almacenar en un arreglo unidimen-
sional los 100 primeros n ́umeros pares. Imprimir al nal el arreglo.*/

#include <stdio.h>

int lista [100];
int i,p;

int main()
{
  printf("\tEste un programa que te ayuda a imprimir los primeros 100 n numeros pares\n\n\n\n");
  for(i=0;i<100;i++)
  {
    lista[i]=2*(i+1);
  }
  printf("Estos son los primeros 100 numeros pares");
  for(i=0;i<100;i++)
  {
   printf("%d\n",lista[i]);
  }
  
}
